# Copy this file to `.env` and fill in the real value.
# The order form POSTs the request as JSON to this URL.
# TODO: paste the real n8n webhook URL here (production URL, not the test one).
PUBLIC_ORDER_WEBHOOK_URL=
